## 1. 固定基准

- [x] 1.1 保存普通直伤与四人梦见月风/冰反应的非私人场景输入，记录主线 SHA、Node 和数据快照；用同一探针保存优化前的基线、候选及事件/来源轨迹，验证可复现运行。
- [x] 1.2 在统一预热后各采样 3 次，记录单指标、完整报告和当前单精炼全量重算的耗时中位数与候选数量；完成独立性能记录，不把 profiler 与非 profiler 时间直接比较。

## 2. 单候选权威链路

- [x] 2.1 提取全量和单候选共同使用的武器资格、条件迁移与求值逻辑，新增 Analyzer 包根入口；用真实 SQLite 集成用例验证普通武器及有队伍增益武器的完整/局部结果一致，并确认不修改输入。
- [x] 2.2 增加共享 TypeBox 单候选请求和响应 Schema；运行 Contracts 测试验证精炼边界、已有完整请求响应兼容及武器结果形状复用。
- [x] 2.3 装配 `/v1/analysis/weapon-comparison`；通过真实 buildApp HTTP 集成验证成功、未知/不兼容武器、不可用候选、低命座及非法精炼，验证局部只求基线与指定候选、不执行其他比较项。

## 3. Web 局部更新

- [x] 3.1 保存报告场景快照并接入单武器请求、行级加载/错误/重试和按 ID 更新排序；通过 Web 集成验证精炼切换不调用完整分析且其余报告不变，相同精炼不重复请求。
- [x] 3.2 加入报告世代与逐武器请求序号及取消清理；通过可控响应顺序验证同武器最后选择有效、不同武器分别合并、场景改变及新完整报告使旧结果失效，失败后不把旧数值标成新精炼。

## 4. 计算准备降本

- [x] 4.1 在只读 GameDataRepository 内复用预编译语句和有界查询结果；运行 GameData 完整测试并验证查询减少、键区分、复合对象不被调用方污染、两数据库隔离及关闭后不可读。
- [x] 4.2 拆分真实动作来源快照和参与者局部上下文；运行已有特殊反应及前后台资格集成用例，与 1.1 的事件/来源轨迹差分确认不变，并验证同一变体不重复准备整队来源映射。
- [x] 4.3 为完整报告复用一次可信基线求值，保留现有 Analyzer 包根兼容入口；通过 API/Analyzer 集成确认 evaluation 与 analysis 基线一致且不再重复求基线。

## 5. 集成验收与交付

- [x] 5.1 运行同输入优化后探针，比较三类耗时中位数、事件结果与来源轨迹；交付优化前后记录及残留瓶颈，确认团队增益随候选重新生效，不依靠修改正确期望值通过。
- [x] 5.2 运行 `pnpm typecheck`、`pnpm build`，以及 Contracts、GameData、Analyzer、Web 全包测试；API 聚焦完整分析、单候选、梦见月反应与来源资格的相关集成/边界测试。各包按序运行避免资源竞争；本变更不涉及 Content 声明、导入、存储或鉴权，不重复全 API 慢速无关矩阵。如发现数值差异或扩大改动范围，先定位再扩展相关测试门禁。
- [x] 5.3 使用本地真实 API 与浏览器完成一次完整报告及多把武器精炼调整，确认请求范围、局部反馈、报告保留与失败重试；记录验证证据，不操作生产用户数据。
- [x] 5.4 更新 ADR-0021 状态与 ADR 索引，并补充 ADR-0014 的子资源说明，确认仍只有一个权威求值模型；运行 `openspec validate optimize-counterfactual-analysis-performance --strict`、`git diff --check`，检查 Git diff/status 并保留无关图片。本任务不包含提交、推送、部署或归档。
