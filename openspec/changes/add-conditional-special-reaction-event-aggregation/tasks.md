## 1. 事件模型与作者守卫

- [x] 1.1 为动作内事件增加实际星扩散变体、`minimumSourceConstellation`和事件级效果筛选声明，并通过 `pnpm --filter @gscombat/content typecheck`验证既有 Content 声明仍兼容
- [x] 1.2 扩展 registry integrity 审计，拒绝越界命座、未知事件 ID、实际星扩散事件误绑角色倍率伤害部分等无效声明，并运行聚焦 registry integrity 系统测试

## 2. 权威求值链路

- [x] 2.1 在动作计划编译中根据每次候选主 Build 的累计命座筛选事件，并用 Analyzer 集成测试验证 C0 排除 C1 事件、C1 至 C6 包含同一事件以及反事实重算不会复用错误计划
- [x] 2.2 将实际星扩散事件接入现有参与者聚合公式，并通过集成测试验证其使用等级基础伤害、触发系数和参与者贡献，而非角色属性倍率直伤公式
- [x] 2.3 将动作效果改为逐事件解析，令事件 ID、元素和特殊反应类型共同参与筛选；通过集成测试验证事件限定的 `specialReactionFlatDamageAddition`只进入目标事件且未限定的既有效果保持原行为
- [x] 2.4 统一聚合实际星扩散与独立星扩散事件的期望伤害，验证事件和等于动作总值，且每段分别执行暴击、抗性与擢升阶段

## 3. 梦见月瑞希内容

- [x] 3.1 在角色目录中声明一个辉映星扩散联动动作与一个现有目标伤害指标，绑定固定 7.0 数据证据：廓然梦生 `1000%精通`、C1 独立事件 `400%精通`和 C1 Fpost `550%精通`；运行 Content 快照与注册表检查
- [x] 3.2 增加 API 级集成测试：C0 返回触发反应与 `1000%精通`事件；C1/C6 额外返回 `400%精通`事件，且仅触发反应轨迹出现 `550%精通`；验证不产生 C1 专属指标
- [x] 3.3 使用固定队伍和敌人请求验证 C1 总值严格等于增强后的触发反应、`400%精通`事件与`1000%精通`事件之和，并验证 C0 总值不含两项 C1 贡献

## 4. 契约与页面解释

- [x] 4.1 检查现有分析响应能否无损承载实际星扩散参与者明细；如不能，仅增加向后兼容的可选判别与明细字段，并运行 Contracts 与 API 兼容测试证明旧请求无新增必填项
- [x] 4.2 在计算轨迹中按事件展示触发反应、C1 `400%精通`与廓然梦生 `1000%精通`并显示合计；以 API 事件轨迹断言和 Web 生产构建验证展示数据与页面兼容

## 5. 回归与交付检查

- [x] 5.1 运行 `pnpm --filter @gscombat/content registries:check`以及相关 Analyzer 与 API 聚焦测试，确认生成注册表和单事件特殊反应指标无回归
- [x] 5.2 按本次聚焦交付运行 `pnpm typecheck`和`pnpm build`；遵循用户要求不追加全量 `pnpm test`
- [x] 5.3 运行 `openspec validate add-conditional-special-reaction-event-aggregation --strict`、`git diff --check`和`git status --short`，确认任务、规格、实现一致并保留用户的无关工作树改动
