## 1. B0 基线、既有修复与完整性

- [x] 1.1 在用户确认方案后启动 apply，记录 HEAD、已有 dirty 文件及 R01–R24 状态；验证 `git status --short` 与 audit.md 基线一致，若不一致先核对新变化。
- [x] 1.2 复核而不覆盖木偶已有修复（R01），验证星超导路由、存储附着参数、攻转精通和 C1/C2；以现有 `special-reaction-scenario.test.ts`、API `moon-and-stellar-team-effects.test.ts` 聚焦用例确认，保持 C4 不合并。
- [x] 1.3 恢复伊涅芙底层普攻直伤声明（R23），在 `combat-registry-integrity.test.ts` 增加公式类别冲突反例；验证混合事件合法动作不被误拒绝。
- [x] 1.4 补奥黛塔两个 C1 部分的审阅证据（R24），执行现有 registry integrity 并确认不再有两条缺失证据；不调整伤害数值掩盖证据问题。

## 2. B1 真实事件、基础区、概率与继承

- [x] 2.1 以流浪者低命加云堇和爱可菲六段加申鹤建立精简集成反例（R02/R03）；验证未解锁/代理节点没有命中贡献、合法固定伤害命中仍被保留。
- [x] 2.2 修复实际角色追加段的合格基础项组装（R03），用参数化集成覆盖绫人、丝柯克普攻/爆发、爱可菲与伊法；断言实际基础项/来源而不只断言总伤害增长。
- [x] 2.3 添加最小显式继承资格，修丝柯克 C2 与基尼奇 C1（R04）；运行 `probes/events.mjs` 的继承反例，并用现有武器独立 proc 用例证明没有误继承父动作专属增益。
- [x] 2.4 分离事件概率与天赋倍率（R06/R03），以伊法50%追加弹与宵宫C6加云堇验证完整事件期望；检查概率0/1及暴击率超100%无额外暴击收益的边界，不建立随机模拟。
- [x] 2.5 将这些反例收敛到 Analyzer 的现有 C6/同命中集成测试组织，执行相关文件及 Calculator 事件测试；检查每段合计等于动作结果，轨迹无代理贡献、无重复加算。

## 3. B2 有效条件与互斥依赖

- [x] 3.1 修依赖解析只看请求ID的问题（R05），验证不合格二层心得选择不能增加单人基尼奇C6伤害；`probes/events.mjs` 对应反例转绿。
- [x] 3.2 保留合法一层/二层和来源选择行为，将基尼奇心得归入对应真实弹跳的同段项；用单人、三纳塔、显式一层三个集成场景检查互斥及事件数量。
- [x] 3.3 检查共享依赖守卫其他消费者的来源/命座/条件失效输入，复用参数化系统用例确认无死循环、无越界增益；不按角色复制同一测试。

## 4. B3 参数、乘区、指标门槛及无反应兼容

- [x] 4.1 修莱欧斯利四项（R07）：重击参数、C1增伤区、C6冰锥强化、C6专属门槛；以固定来源映射、带增伤杯的对照和C5/C6入口验证，不能仅改快照值。
- [x] 4.2 修希诺宁C6防御倍率及C4团队资格（R08/R09）；验证纯攻击扰动不提高该夜魂基础项、合格队友普重下落能取得C4、不合格技能不取得。
- [x] 4.3 修玛薇卡四个无反应别名（R10），消除重复公开选项并保留兼容解释；API级验证旧ID与规范无反应ID一致且非伪零，沿相同公共路径检查其他既有变体。
- [x] 4.4 修林尼C6、莱依拉Q、魈高空下落参数/属性（R11–R13），用 `probes/scaling.mjs` 和小型语义映射集成表验证；保留正确C3/C5天赋累计。
- [x] 4.5 为魈C6免费E补正确状态下的A1/A4（R14），按已同意最大可达约定展示明确层数；验证开Q至少A1生效，E不吃Q的普重下落专属加成。
- [x] 4.6 刷新必要语义证据/生成注册表，执行 `pnpm --filter @gscombat/content registries:check` 和 Content 完整测试；API/Web相关入口验证低命专属指标不可选且直接请求被拒绝。

## 5. B4 辅助来源面板与治疗护盾

- [x] 5.1 统一辅助来源 automatic/maximum_reachable 角色属性解析（R16），以迪奥娜C5/C6来源HP对照和条件不成立负例验证，不把其他接收者增益混入来源。
- [x] 5.2 修迪奥娜Q HP缩放及早柚C6附加治疗（R15/R17），在 `metrics/metric.test.ts` 或现有治疗系统集成中验证攻击/生命/精通扰动、C5/C6和早柚6000上限。
- [x] 5.3 修梦见月本人点心治疗×2（R20），比较同来源给本人/队友并带非零治疗加成的结果，确认不是错误的治疗加成池叠加。
- [x] 5.4 实现或复用完整基础盾倍率，补莱依拉C1和迪奥娜C2（R21/R22）；验证包含固定值的整盾×1.2/×1.15，保持当前点按与非冰伤口径。
- [x] 5.5 通过 `/v1/support-metrics/evaluate` 聚焦组合验证辅助结果/公式一致；若增加可选契约字段，执行 contracts 兼容测试，确认旧请求无需新必填字段、用户Build无需迁移。

## 6. B5 普通扩散与前台互斥

- [x] 6.1 接入梦见月普通扩散C1后置固定伤害（R18），明确当前辉映分支；用C0/C1/C6集成验证该次触发的11×EM与星扩散5.5×EM不串段。
- [x] 6.2 为需要的效果/指标增加最小静态前后台资格（R19），验证奥黛塔前台破晓终奏不能获得互斥梦浮效果，同时真实后台队友仍能受益；不得建实时状态机。
- [x] 6.3 对精通、反应增伤、减抗及反应双暴统一检查梦浮资格，执行现有月/星反应API聚焦用例；保留梦见月本人的完整指标效果与木偶C4不合并约定。

## 7. B6 集成验收与交付

- [x] 7.0 处理全测新增确认的 R25：来源精通拆分漏队友装备固定精通，恢复千夜浮梦等合格团队装备贡献；保留玛海菈/晚星原有真实集成反例，排除递归精通分享及自身装备重复叠加。

- [x] 7.1 重跑302指标C0–C6入口扫描，对比 `scan-results.json`，逐一解释指标去重/新增资格导致的计数变化；不以“全返回正数”作为公式正确性标准。
- [x] 7.2 选少量受影响指标执行完整 `analyzeScenario`/HTTP分析，确认武器候选、词条边际、有效词条与修复后的基线一致；验证无关属性收益为零或符合明确转换关系。
- [x] 7.3 执行相关 Analyzer/API/Web 集成文件及 Content完整测试后，统一运行 `pnpm typecheck`、`pnpm test`、`pnpm build` 全仓门禁；有失败先定位，不修改正确期望掩盖差异。
- [x] 7.4 执行 `openspec validate audit-and-repair-combat-metric-regressions --strict`、`git diff --check`、`git status --short`；逐项核对R01–R24实施/验证状态，待核实项单列，不宣告旧change整体完成。
- [x] 7.5 向用户交付已修项、未决项、验证证据与部署状态；ADR-0020仅在方案被批准且实现符合时转为已采纳，未经单独授权不提交、推送、部署或归档。

## 8. B7 用户追加批准的两项核实

- [x] 8.1 核实艾梅莉埃 C6 普重精馏：沿用 active Buff 明确目标燃烧前提，按实际攻击转换且封顶36%，不与默认燃烧二阶指标重复；用相关场景集成验证开关、上限和属性扰动。
- [x] 8.2 用卡齐娜 C6 与叶洛亚岩伤加算复现代理影响；若确认占位命中重复取得加算，沿用非命中代理修复，保留真实200%防御伤害、C6门槛及真实命中加算。
- [x] 8.3 更新 audit/implementation，绫人C6/C1按用户要求暂缓；本次仅内容声明与已有集成用例增补，执行 Content测试/构建、Analyzer相关集成和两包类型检查、OpenSpec校验及diff检查，不重复刚完成的全仓发布门禁；不提交、不部署。

## 9. B8 用户批准的梦见月星扩散拆分

- [x] 9.1 修正直伤并新增单次风反应指标，迁移C1后置加算，保留旧ID与普通扩散/治疗，明确固定前提。
- [x] 9.2 支持纯反应时间线、固定冰底贡献者筛选及真实前台身份，保留原贡献排序；以少量C0/C1/C6跨包集成验证两类事件互不混算。
- [x] 9.3 执行Content完整测试/注册表、Analyzer相关集成与系统守卫、API完整分析及受影响包类型检查/构建、OpenSpec与diff检查；本批不重复全仓发布门禁，不提交部署。

## 10. B9 元素限定贡献和冰风旋

- [x] 10.1 落实用户裁决的风/冰权重位置和逐暴击组合求期望；缺位为零，月反应与直伤不变，以少量聚合公式反例验证。
- [x] 10.2 新增冰风旋指标及1–6级参数（默认6），贯通事件解析/作者审计/响应可选等级字段/前端轨迹，保留C1及梦浮资格；集成验证两档、非法参数和贡献者。
- [x] 10.3 运行相关Calculator/Content/Contracts测试、Analyzer及API聚焦集成、Web参数与轨迹测试，受影响包类型检查/构建、OpenSpec严格校验及diff检查；不重复全仓发布门禁，不提交部署。
